# Clearinghouse

# Usage

```bash
# Start Postgres DB to store puzzle data
bash start.sh

# Stop Postgres DB to store puzzle data
bash stop.sh

# Delete Postgres DB to store puzzle data
bash delete.sh
```
